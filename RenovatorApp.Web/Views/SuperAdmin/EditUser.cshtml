@model RenovatorApp.Web.ViewModels.SuperAdminUserEditViewModel

@{
    ViewData["Title"] = Model.IsEditMode ? "Edit User" : "Add User";
}

<div class="page-header-row">
    <div>
        <h1>@(Model.IsEditMode ? "Edit User" : "Add User")</h1>
        <p class="text-muted">@(Model.IsEditMode ? "Update user information and roles." : "Create a new user.")</p>
    </div>
    <a class="btn btn-outline-secondary" asp-controller="SuperAdmin" asp-action="Index">Back</a>
</div>

<form asp-controller="SuperAdmin"
      asp-action="@(Model.IsEditMode ? "EditUser" : "AddUser")"
      asp-route-id="@Model.UserID"
      method="post">
    <input asp-for="UserID" type="hidden" />
    <input asp-for="RenoCompanyID" type="hidden" />

    @if (!ViewData.ModelState.IsValid)
    {
        <div class="alert alert-warning" role="alert">
            Fix the highlighted fields before saving.
        </div>
    }

    <div class="detail-grid">
        <section class="detail-section detail-section-full">
            <h2>User</h2>
            <div class="row g-3">
                <div class="col-12 col-lg-4">
                    <label class="form-label" asp-for="Login"></label>
                    <input class="form-control" asp-for="Login" autocomplete="username" />
                    <span class="text-danger" asp-validation-for="Login"></span>
                </div>
                <div class="col-12 col-md-6 col-lg-4">
                    <label class="form-label" asp-for="FirstName"></label>
                    <input class="form-control" asp-for="FirstName" />
                    <span class="text-danger" asp-validation-for="FirstName"></span>
                </div>
                <div class="col-12 col-md-6 col-lg-4">
                    <label class="form-label" asp-for="LastName"></label>
                    <input class="form-control" asp-for="LastName" />
                    <span class="text-danger" asp-validation-for="LastName"></span>
                </div>
                <div class="col-12 col-lg-4">
                    <label class="form-label" asp-for="Email"></label>
                    <input class="form-control" asp-for="Email" autocomplete="email" />
                </div>
                <div class="col-12 col-md-6 col-lg-4">
                    <label class="form-label" asp-for="PhonePrimary"></label>
                    <input class="form-control" asp-for="PhonePrimary" />
                </div>
                <div class="col-12 col-md-6 col-lg-4">
                    <label class="form-label" asp-for="PhoneSecondary"></label>
                    <input class="form-control" asp-for="PhoneSecondary" />
                </div>
                <div class="col-12 col-lg-4">
                    <label class="form-label">RenoCompany</label>
                    <input class="form-control" value="@Model.RenoCompanyName" readonly />
                </div>
                <div class="col-12">
                    <div class="form-check">
                        <input class="form-check-input" asp-for="Active" />
                        <label class="form-check-label" asp-for="Active"></label>
                    </div>
                </div>
            </div>
        </section>

        <section class="detail-section">
            <div class="detail-section-title-row">
                <h2>Password</h2>
                <button class="btn btn-outline-secondary btn-sm password-toggle-button" type="button">Show</button>
            </div>
            <p class="text-muted">@(Model.IsEditMode ? "Leave blank to keep the current password." : "Password is required for new users.")</p>
            <div class="mb-3">
                <label class="form-label" asp-for="Password"></label>
                <input class="form-control password-toggle-field" asp-for="Password" autocomplete="new-password" />
                <span class="text-danger" asp-validation-for="Password"></span>
            </div>
            <div>
                <label class="form-label" asp-for="ConfirmPassword"></label>
                <input class="form-control password-toggle-field" asp-for="ConfirmPassword" autocomplete="new-password" />
                <span class="text-danger" asp-validation-for="ConfirmPassword"></span>
            </div>
        </section>

        <section class="detail-section">
            <h2>Roles</h2>
            <div class="plain-list">
                @foreach (var role in Model.AvailableRoles)
                {
                    <label class="form-check">
                        <input class="form-check-input"
                               name="SelectedRoleIDs"
                               type="checkbox"
                               value="@role.RoleID"
                               @(Model.SelectedRoleIDs.Contains(role.RoleID) ? "checked" : string.Empty) />
                        <span class="form-check-label">@role.Name</span>
                    </label>
                }
            </div>
        </section>
    </div>

    <div class="d-flex gap-2 mt-3">
        <button class="btn btn-primary" type="submit">Save</button>
        <a class="btn btn-outline-secondary" asp-controller="SuperAdmin" asp-action="Users">Cancel</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        const passwordToggleButton = document.querySelector('.password-toggle-button');
        const passwordToggleFields = document.querySelectorAll('.password-toggle-field');

        passwordToggleButton?.addEventListener('click', () => {
            const showPasswords = passwordToggleFields[0]?.type === 'password';
            passwordToggleFields.forEach((field) => {
                field.type = showPasswords ? 'text' : 'password';
            });
            passwordToggleButton.textContent = showPasswords ? 'Hide' : 'Show';
        });
    </script>
}
